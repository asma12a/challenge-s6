// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/asma12a/challenge-s6/ent/runningevent"
	"github.com/asma12a/challenge-s6/ent/schema/ulid"
)

// RunningEvent is the model entity for the RunningEvent schema.
type RunningEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID ulid.ID `json:"id,omitempty"`
	// EventRunningID holds the value of the "event_running_id" field.
	EventRunningID string `json:"event_running_id,omitempty"`
	// TeamID holds the value of the "team_id" field.
	TeamID       string `json:"team_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RunningEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case runningevent.FieldEventRunningID, runningevent.FieldTeamID:
			values[i] = new(sql.NullString)
		case runningevent.FieldID:
			values[i] = new(ulid.ID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RunningEvent fields.
func (re *RunningEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case runningevent.FieldID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				re.ID = *value
			}
		case runningevent.FieldEventRunningID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_running_id", values[i])
			} else if value.Valid {
				re.EventRunningID = value.String
			}
		case runningevent.FieldTeamID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field team_id", values[i])
			} else if value.Valid {
				re.TeamID = value.String
			}
		default:
			re.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RunningEvent.
// This includes values selected through modifiers, order, etc.
func (re *RunningEvent) Value(name string) (ent.Value, error) {
	return re.selectValues.Get(name)
}

// Update returns a builder for updating this RunningEvent.
// Note that you need to call RunningEvent.Unwrap() before calling this method if this RunningEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (re *RunningEvent) Update() *RunningEventUpdateOne {
	return NewRunningEventClient(re.config).UpdateOne(re)
}

// Unwrap unwraps the RunningEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (re *RunningEvent) Unwrap() *RunningEvent {
	_tx, ok := re.config.driver.(*txDriver)
	if !ok {
		panic("ent: RunningEvent is not a transactional entity")
	}
	re.config.driver = _tx.drv
	return re
}

// String implements the fmt.Stringer.
func (re *RunningEvent) String() string {
	var builder strings.Builder
	builder.WriteString("RunningEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", re.ID))
	builder.WriteString("event_running_id=")
	builder.WriteString(re.EventRunningID)
	builder.WriteString(", ")
	builder.WriteString("team_id=")
	builder.WriteString(re.TeamID)
	builder.WriteByte(')')
	return builder.String()
}

// RunningEvents is a parsable slice of RunningEvent.
type RunningEvents []*RunningEvent
