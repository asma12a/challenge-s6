// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/asma12a/challenge-s6/ent/basketevent"
	"github.com/asma12a/challenge-s6/ent/schema/ulid"
)

// BasketEvent is the model entity for the BasketEvent schema.
type BasketEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID ulid.ID `json:"id,omitempty"`
	// EventBasketID holds the value of the "event_basket_id" field.
	EventBasketID string `json:"event_basket_id,omitempty"`
	// TeamA holds the value of the "team_A" field.
	TeamA string `json:"team_A,omitempty"`
	// TeamB holds the value of the "team_B" field.
	TeamB        string `json:"team_B,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BasketEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case basketevent.FieldEventBasketID, basketevent.FieldTeamA, basketevent.FieldTeamB:
			values[i] = new(sql.NullString)
		case basketevent.FieldID:
			values[i] = new(ulid.ID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BasketEvent fields.
func (be *BasketEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case basketevent.FieldID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				be.ID = *value
			}
		case basketevent.FieldEventBasketID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_basket_id", values[i])
			} else if value.Valid {
				be.EventBasketID = value.String
			}
		case basketevent.FieldTeamA:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field team_A", values[i])
			} else if value.Valid {
				be.TeamA = value.String
			}
		case basketevent.FieldTeamB:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field team_B", values[i])
			} else if value.Valid {
				be.TeamB = value.String
			}
		default:
			be.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BasketEvent.
// This includes values selected through modifiers, order, etc.
func (be *BasketEvent) Value(name string) (ent.Value, error) {
	return be.selectValues.Get(name)
}

// Update returns a builder for updating this BasketEvent.
// Note that you need to call BasketEvent.Unwrap() before calling this method if this BasketEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (be *BasketEvent) Update() *BasketEventUpdateOne {
	return NewBasketEventClient(be.config).UpdateOne(be)
}

// Unwrap unwraps the BasketEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (be *BasketEvent) Unwrap() *BasketEvent {
	_tx, ok := be.config.driver.(*txDriver)
	if !ok {
		panic("ent: BasketEvent is not a transactional entity")
	}
	be.config.driver = _tx.drv
	return be
}

// String implements the fmt.Stringer.
func (be *BasketEvent) String() string {
	var builder strings.Builder
	builder.WriteString("BasketEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", be.ID))
	builder.WriteString("event_basket_id=")
	builder.WriteString(be.EventBasketID)
	builder.WriteString(", ")
	builder.WriteString("team_A=")
	builder.WriteString(be.TeamA)
	builder.WriteString(", ")
	builder.WriteString("team_B=")
	builder.WriteString(be.TeamB)
	builder.WriteByte(')')
	return builder.String()
}

// BasketEvents is a parsable slice of BasketEvent.
type BasketEvents []*BasketEvent
