// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/asma12a/challenge-s6/ent/predicate"
	"github.com/asma12a/challenge-s6/ent/schema/ulid"
	"github.com/asma12a/challenge-s6/ent/tennisevent"
)

// TennisEventQuery is the builder for querying TennisEvent entities.
type TennisEventQuery struct {
	config
	ctx        *QueryContext
	order      []tennisevent.OrderOption
	inters     []Interceptor
	predicates []predicate.TennisEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TennisEventQuery builder.
func (teq *TennisEventQuery) Where(ps ...predicate.TennisEvent) *TennisEventQuery {
	teq.predicates = append(teq.predicates, ps...)
	return teq
}

// Limit the number of records to be returned by this query.
func (teq *TennisEventQuery) Limit(limit int) *TennisEventQuery {
	teq.ctx.Limit = &limit
	return teq
}

// Offset to start from.
func (teq *TennisEventQuery) Offset(offset int) *TennisEventQuery {
	teq.ctx.Offset = &offset
	return teq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (teq *TennisEventQuery) Unique(unique bool) *TennisEventQuery {
	teq.ctx.Unique = &unique
	return teq
}

// Order specifies how the records should be ordered.
func (teq *TennisEventQuery) Order(o ...tennisevent.OrderOption) *TennisEventQuery {
	teq.order = append(teq.order, o...)
	return teq
}

// First returns the first TennisEvent entity from the query.
// Returns a *NotFoundError when no TennisEvent was found.
func (teq *TennisEventQuery) First(ctx context.Context) (*TennisEvent, error) {
	nodes, err := teq.Limit(1).All(setContextOp(ctx, teq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tennisevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (teq *TennisEventQuery) FirstX(ctx context.Context) *TennisEvent {
	node, err := teq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TennisEvent ID from the query.
// Returns a *NotFoundError when no TennisEvent ID was found.
func (teq *TennisEventQuery) FirstID(ctx context.Context) (id ulid.ID, err error) {
	var ids []ulid.ID
	if ids, err = teq.Limit(1).IDs(setContextOp(ctx, teq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tennisevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (teq *TennisEventQuery) FirstIDX(ctx context.Context) ulid.ID {
	id, err := teq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TennisEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TennisEvent entity is found.
// Returns a *NotFoundError when no TennisEvent entities are found.
func (teq *TennisEventQuery) Only(ctx context.Context) (*TennisEvent, error) {
	nodes, err := teq.Limit(2).All(setContextOp(ctx, teq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tennisevent.Label}
	default:
		return nil, &NotSingularError{tennisevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (teq *TennisEventQuery) OnlyX(ctx context.Context) *TennisEvent {
	node, err := teq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TennisEvent ID in the query.
// Returns a *NotSingularError when more than one TennisEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (teq *TennisEventQuery) OnlyID(ctx context.Context) (id ulid.ID, err error) {
	var ids []ulid.ID
	if ids, err = teq.Limit(2).IDs(setContextOp(ctx, teq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tennisevent.Label}
	default:
		err = &NotSingularError{tennisevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (teq *TennisEventQuery) OnlyIDX(ctx context.Context) ulid.ID {
	id, err := teq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TennisEvents.
func (teq *TennisEventQuery) All(ctx context.Context) ([]*TennisEvent, error) {
	ctx = setContextOp(ctx, teq.ctx, ent.OpQueryAll)
	if err := teq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TennisEvent, *TennisEventQuery]()
	return withInterceptors[[]*TennisEvent](ctx, teq, qr, teq.inters)
}

// AllX is like All, but panics if an error occurs.
func (teq *TennisEventQuery) AllX(ctx context.Context) []*TennisEvent {
	nodes, err := teq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TennisEvent IDs.
func (teq *TennisEventQuery) IDs(ctx context.Context) (ids []ulid.ID, err error) {
	if teq.ctx.Unique == nil && teq.path != nil {
		teq.Unique(true)
	}
	ctx = setContextOp(ctx, teq.ctx, ent.OpQueryIDs)
	if err = teq.Select(tennisevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (teq *TennisEventQuery) IDsX(ctx context.Context) []ulid.ID {
	ids, err := teq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (teq *TennisEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, teq.ctx, ent.OpQueryCount)
	if err := teq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, teq, querierCount[*TennisEventQuery](), teq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (teq *TennisEventQuery) CountX(ctx context.Context) int {
	count, err := teq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (teq *TennisEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, teq.ctx, ent.OpQueryExist)
	switch _, err := teq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (teq *TennisEventQuery) ExistX(ctx context.Context) bool {
	exist, err := teq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TennisEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (teq *TennisEventQuery) Clone() *TennisEventQuery {
	if teq == nil {
		return nil
	}
	return &TennisEventQuery{
		config:     teq.config,
		ctx:        teq.ctx.Clone(),
		order:      append([]tennisevent.OrderOption{}, teq.order...),
		inters:     append([]Interceptor{}, teq.inters...),
		predicates: append([]predicate.TennisEvent{}, teq.predicates...),
		// clone intermediate query.
		sql:  teq.sql.Clone(),
		path: teq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EventTennisID string `json:"event_tennis_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TennisEvent.Query().
//		GroupBy(tennisevent.FieldEventTennisID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (teq *TennisEventQuery) GroupBy(field string, fields ...string) *TennisEventGroupBy {
	teq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TennisEventGroupBy{build: teq}
	grbuild.flds = &teq.ctx.Fields
	grbuild.label = tennisevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EventTennisID string `json:"event_tennis_id,omitempty"`
//	}
//
//	client.TennisEvent.Query().
//		Select(tennisevent.FieldEventTennisID).
//		Scan(ctx, &v)
func (teq *TennisEventQuery) Select(fields ...string) *TennisEventSelect {
	teq.ctx.Fields = append(teq.ctx.Fields, fields...)
	sbuild := &TennisEventSelect{TennisEventQuery: teq}
	sbuild.label = tennisevent.Label
	sbuild.flds, sbuild.scan = &teq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TennisEventSelect configured with the given aggregations.
func (teq *TennisEventQuery) Aggregate(fns ...AggregateFunc) *TennisEventSelect {
	return teq.Select().Aggregate(fns...)
}

func (teq *TennisEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range teq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, teq); err != nil {
				return err
			}
		}
	}
	for _, f := range teq.ctx.Fields {
		if !tennisevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if teq.path != nil {
		prev, err := teq.path(ctx)
		if err != nil {
			return err
		}
		teq.sql = prev
	}
	return nil
}

func (teq *TennisEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TennisEvent, error) {
	var (
		nodes = []*TennisEvent{}
		_spec = teq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TennisEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TennisEvent{config: teq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, teq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (teq *TennisEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := teq.querySpec()
	_spec.Node.Columns = teq.ctx.Fields
	if len(teq.ctx.Fields) > 0 {
		_spec.Unique = teq.ctx.Unique != nil && *teq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, teq.driver, _spec)
}

func (teq *TennisEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tennisevent.Table, tennisevent.Columns, sqlgraph.NewFieldSpec(tennisevent.FieldID, field.TypeString))
	_spec.From = teq.sql
	if unique := teq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if teq.path != nil {
		_spec.Unique = true
	}
	if fields := teq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tennisevent.FieldID)
		for i := range fields {
			if fields[i] != tennisevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := teq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := teq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := teq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := teq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (teq *TennisEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(teq.driver.Dialect())
	t1 := builder.Table(tennisevent.Table)
	columns := teq.ctx.Fields
	if len(columns) == 0 {
		columns = tennisevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if teq.sql != nil {
		selector = teq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if teq.ctx.Unique != nil && *teq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range teq.predicates {
		p(selector)
	}
	for _, p := range teq.order {
		p(selector)
	}
	if offset := teq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := teq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TennisEventGroupBy is the group-by builder for TennisEvent entities.
type TennisEventGroupBy struct {
	selector
	build *TennisEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tegb *TennisEventGroupBy) Aggregate(fns ...AggregateFunc) *TennisEventGroupBy {
	tegb.fns = append(tegb.fns, fns...)
	return tegb
}

// Scan applies the selector query and scans the result into the given value.
func (tegb *TennisEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tegb.build.ctx, ent.OpQueryGroupBy)
	if err := tegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TennisEventQuery, *TennisEventGroupBy](ctx, tegb.build, tegb, tegb.build.inters, v)
}

func (tegb *TennisEventGroupBy) sqlScan(ctx context.Context, root *TennisEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tegb.fns))
	for _, fn := range tegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tegb.flds)+len(tegb.fns))
		for _, f := range *tegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TennisEventSelect is the builder for selecting fields of TennisEvent entities.
type TennisEventSelect struct {
	*TennisEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tes *TennisEventSelect) Aggregate(fns ...AggregateFunc) *TennisEventSelect {
	tes.fns = append(tes.fns, fns...)
	return tes
}

// Scan applies the selector query and scans the result into the given value.
func (tes *TennisEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tes.ctx, ent.OpQuerySelect)
	if err := tes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TennisEventQuery, *TennisEventSelect](ctx, tes.TennisEventQuery, tes, tes.inters, v)
}

func (tes *TennisEventSelect) sqlScan(ctx context.Context, root *TennisEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tes.fns))
	for _, fn := range tes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
